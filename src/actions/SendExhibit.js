/**
 * @title send exhibit to
 * @category LindseyDialogue
 * @author OnisBrown.
 */
/** const virtual_machine = (bp: SDK, user, session, temp, bot, event, args) => { */
const axios
var msg = session.slots.Exhibits.value;







/** } */
